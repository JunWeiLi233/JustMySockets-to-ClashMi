"""Test package for subscription-converter.

Tests live alongside the package under ``subscription-converter/tests``.
``pyproject.toml`` adds ``subscription-converter`` to ``pythonpath`` so the
package imports cleanly without an installed distribution.
"""
