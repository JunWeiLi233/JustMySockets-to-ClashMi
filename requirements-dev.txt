# Development dependencies for subscription-converter.
#
# Install on top of the runtime requirements:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# These pins mirror pyproject.toml [project.optional-dependencies].dev.

-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.24,<0.30
respx>=0.21,<0.30
ruff>=0.8,<0.12
mypy>=1.13,<2.0
pre-commit>=4.0,<5.0
types-PyYAML>=6.0,<7.0
